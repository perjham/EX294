---
- name: 'TASK 23: VAULT'
  hosts: all
  become: True
  vars_files:
    - ./23-cloudpass
  vault_password_file:
    - ./23-vaultpass
  tasks:
    - name: Create file
      with_items: "{{ variables }}"
      lineinfile:
        line: "{{ item.varname }} = {{ item.value }}"
        path: /root/cloudcreds.txt

  
